name: Lint

on:
  push:
    branches-ignore:
      - main

jobs:
  print_hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello, World!"

      - name: Checkout
        uses: actions/checkout@v3